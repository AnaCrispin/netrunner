// face-api.min.js - Muestra
console.log("face-api.js cargado correctamente.");
window.faceapi = {
  nets: {
    tinyFaceDetector: {
      loadFromUri: async () => console.log("Modelo tinyFaceDetector cargado"),
    },
    faceLandmark68Net: {
      loadFromUri: async () => console.log("Modelo faceLandmark68Net cargado"),
    },
  },
  TinyFaceDetectorOptions: function () {
    return {};
  },
  detectSingleFace: async function () {
    return {
      detection: { box: { x: 200, y: 100 } },
      withFaceLandmarks: () => Promise.resolve({ detection: { box: { x: 200, y: 100 } } }),
    };
  }
};
